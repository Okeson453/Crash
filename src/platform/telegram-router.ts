/**
 * Thin entry — implementation lives in telegram-router-impl.ts
 * (split for reliable GitHub pushes of large control-plane surface).
 */
export { TenantRouterBot } from './telegram-router-impl.js';
