import type { BetStatus } from '@/types/bet';
import { Select } from '@/components/ui/Select';
export function HistoryFilters({ status, range, onStatus, onRange }: { status?: BetStatus; range: string; onStatus: (status: BetStatus | null) => void; onRange: (range: string) => void }) { return <div className="grid grid-cols-2 gap-2"><Select aria-label="Bet status" value={status ?? 'all'} onChange={(e) => onStatus(e.target.value === 'all' ? null : e.target.value as BetStatus)}><option value="all">All results</option><option value="active">Active</option><option value="cashed_out">Won</option><option value="lost">Lost</option><option value="cancelled">Cancelled</option></Select><Select aria-label="Date range" value={range} onChange={(e) => onRange(e.target.value)}><option value="all">All time</option><option value="today">Today</option><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option></Select></div>; }
