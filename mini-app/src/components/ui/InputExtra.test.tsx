import { render, screen } from '@testing-library/react'; import { TextArea } from './TextArea'; import { Checkbox } from './Checkbox'; import { RadioGroup } from './RadioGroup'; import { Tooltip } from './Tooltip'; import { Divider } from './Divider'; import { Stat } from './Stat'; import { describe, expect, it } from 'vitest';
describe('ui primitives',()=>{it('renders text area',()=>{render(<TextArea aria-label="Message"/>);expect(screen.getByRole('textbox',{name:'Message'})).toBeInTheDocument();});it('renders checkbox',()=>{render(<Checkbox aria-label="Agree"/>);expect(screen.getByRole('checkbox',{name:'Agree'})).toBeInTheDocument();});it('renders radio group',()=>{render(<RadioGroup name="x" value="a" options={[{value:'a',label:'A'}]} onChange={()=>undefined}/>);expect(screen.getByRole('radio',{name:'A'})).toBeChecked();});it('renders tooltip content',()=>{render(<Tooltip label="Hint"><span>Value</span></Tooltip>);expect(screen.getByTitle('Hint')).toBeInTheDocument();});it('renders divider and stat',()=>{render(<><Divider/><Stat label="Count" value={2}/></>);expect(screen.getByText('Count')).toBeInTheDocument();});});
